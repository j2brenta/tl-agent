# Phase 7 compose — prompt evolution

Drafts the per-hot-spot artifact (NOTE/STANDUP/DM/ESCALATE body) using the
TL's tone preferences. Higher temperature (0.2) than other phases — the
text needs to read like a human, not a transcript.

## Versions

- `v1.md` (2026-05-22) — initial. Inlines per-mode format guidance.
